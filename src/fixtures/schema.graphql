type Query {
  int: Int
  float: Float
  boolean: Boolean
  string: String
  map: Map
  scalarArray: [String]
  mapArray: [Map]
  withArgs ( 
    int: Int
    float: Float
    boolean: Boolean
    string: String
  ): String
}

type Map {
  id: ID
  map: Map
  maps: [Map]
}