query Basic {
  int
  float
  boolean
  string
  withArgs
  scalarArray
  mapArray {
    id
    map {
      id
      maps {
        id
      }
    }
  }
  map {
    id
    map {
      id
      map {
        id
      }
    }
  }
}