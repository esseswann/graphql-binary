query Basic {
  int
  float
  boolean
  string
  withArgs
  scalarArray
  mapArray {
    id
    map {
      id
      map {
        id
      }
    }
  }
  map {
    id
    map {
      id
      map {
        id
      }
    }
  }
}